# Node
node_modules/
.next/
out/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment - NEVER commit secrets
.env
.env.local
.env.production
.web/.env
web/.env.local
web/.env.production
serviceAccountKey.json
*.pem
*.key

# Logs
*.log

# VS Code
.vscode/

# OS
.DS_Store
Thumbs.db

# InsightFace model files
# The two REQUIRED files are committed to git (det_500m.onnx, w600k_mbf.onnx)
# so Render can deploy the model bundle without runtime downloads.
# Larger auxiliary models are intentionally ignored to keep the repo small.
models/buffalo_s/1k3d68.onnx
models/buffalo_s/2d106det.onnx
models/buffalo_s/genderage.onnx

